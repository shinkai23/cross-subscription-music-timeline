# Legacy Backend API

旧 Fastify / Prisma backend の退避先です。

現在の Backend は `backend/api` の FastAPI, SQLAlchemy, Alembic, PostgreSQL 構成です。新規実装、CI、ローカル開発手順は `backend/api` を参照します。

このディレクトリは過去実装や設計判断を確認するための場所であり、現行 Backend としては扱いません。
